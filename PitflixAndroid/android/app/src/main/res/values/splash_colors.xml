<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Kept in its own file (not colors.xml) since flutter_launcher_icons
         regenerates colors.xml and would drop this otherwise. Matches
         AppColors.bgBase so the native pre-Flutter splash and the Dart
         SplashScreen widget hand off without a visible color flash. -->
    <color name="splash_background">#0A0614</color>
</resources>
