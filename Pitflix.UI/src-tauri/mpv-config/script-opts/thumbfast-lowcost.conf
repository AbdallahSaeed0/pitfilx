# Thumbfast configuration - LOW COST DIAGNOSTIC PROFILE
# Disable thumbnail generation to reduce CPU/IO load

# Disable thumbnail generation entirely
# (mpv doesn't have a direct disable, so we make it minimal)
max_height=32
max_width=32
# Don't spawn on first load
spawn_first=no
# No hardware decode for thumbnails
hwdec=no
