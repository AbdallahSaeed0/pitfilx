import type { ReactNode } from "react";

/** Placeholder — wire Framer Motion hover preview in a later pass. */
export function HoverCard({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
